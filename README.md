# linux-backup

Make snapshot backups using `rsync`.

## Installation

### Requirements

`rsync`

### Install

```
git clone --depth 1 https://github.com/dpopchev/linux-backup
cd linux-backup
make install
```

## Usage
